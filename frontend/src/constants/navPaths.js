// 내비게이션 메뉴 key → 실제 라우트 경로 매핑.
// SideNav(PC)와 MobileNav(Drawer/BottomNav)가 같은 메뉴 목록을 쓰므로 여기서 한 번만 정의한다.
export const NAV_PATHS = {
  dash: '/',
  report: '/report',
  cat: '/category',
  wiki: '/wiki',
  agent: '/agent',
  settings: '/settings',
};
