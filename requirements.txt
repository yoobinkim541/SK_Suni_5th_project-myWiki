# Agent · API
fastapi>=0.115
uvicorn[standard]>=0.32
anthropic>=0.40
supabase>=2.9
pydantic>=2.9
pyjwt>=2.9
python-dotenv>=1.0

# collectors · preprocessing (데이터 파이프라인)
httpx>=0.27          # 외부 소스 HTTP 요청 (supabase가 이미 의존하지만 직접 쓰므로 명시)
feedparser>=6.0      # RSS/Atom 파싱 (GeekNews·구글 RSS)
beautifulsoup4>=4.12 # HTML 노이즈 제거. 파서는 표준 html.parser를 쓰므로 lxml은 불필요
markdownify>=0.13    # HTML -> Markdown 변환
pypdf>=5.0           # PDF 텍스트 추출 (application/pdf 소스)

# 테스트
pytest>=8.0
