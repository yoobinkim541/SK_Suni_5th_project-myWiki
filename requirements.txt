fastapi>=0.115
uvicorn[standard]>=0.32
anthropic>=0.40
openai>=1.99  # OpenRouter는 OpenAI 호환 API — base_url만 교체해서 사용
supabase>=2.9
pydantic>=2.9
pyjwt[crypto]>=2.9    # [crypto] 필수 — Supabase JWT Signing Keys(ES256) 검증에 씀
python-dotenv>=1.0
reportlab>=4.2
pypdf>=5.0
python-docx>=1.1
python-pptx>=1.0

# collectors · preprocessing (데이터 파이프라인)
httpx>=0.27               # 외부 소스 HTTP 요청
feedparser>=6.0           # RSS/Atom 파싱 (GeekNews·구글 RSS)
beautifulsoup4>=4.12      # HTML 노이즈 제거 (표준 html.parser 사용 -> lxml 불필요)
markdownify>=0.13         # HTML -> Markdown 변환
googlenewsdecoder>=0.1.7  # 구글 뉴스 RSS 링크(JS 리다이렉트) -> 언론사 원문 주소 디코딩
pywebpush>=2.0            # 위키 발행 브라우저 푸시 알림(표준 Web Push, VAPID)

# 테스트
pytest>=8.0
